{% if app.user == true %}
<ul class="nav pull-right">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'AppFOSUserBundle') }}
            </a>
            <ul class="dropdown-menu">
                <li><a href="{{ path('sonata_user_profile_show') }}">Ver Mi Perfil</a></li>
                <li><a href="{{ path('fos_user_change_password') }}">Cambiar Contraseña</a></li>
                <li class="divider"></li>
                {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('sonata_admin_dashboard') }}">Panel de Administración</a></li>
                <li class="divider"></li>
                {% endif %}
                <li>
                    <a href="{{ path('fos_user_security_logout') }}">{{ 'layout.logout'|trans({}, 'AppFOSUserBundle') }}</a>
                </li>
            </ul>
        </li>    
    </ul>
    {% endif %}

    {% if app.user == false %}

    <ul class="nav pull-right" style="margin-left: 35px;">
        <li class="dropdown dropdown-toggle">
            <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                 {{ 'security.login.submit'|trans({}, 'AppFOSUserBundle') }}
                </a>
                <ul class="dropdown-menu">
                    <li><a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'AppFOSUserBundle') }}</a></li>
                    {#
                    <li class="divider"></li>
                    <li><a href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'AppFOSUserBundle') }}</a></li>
                    #}
                </ul>
            </li>    
        </ul>
{% endif %}
